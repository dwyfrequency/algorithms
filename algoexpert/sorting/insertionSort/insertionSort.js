const insertionSort = arr => {
  if (!arr.length) return arr;

  let sorted = false;
  const newArr = [];
  while (!sorted) {
    for (let i = 0; i < arr.length; i++) {}
  }
};

module.exports = {
  insertionSort,
};
